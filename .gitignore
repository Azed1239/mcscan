# Scan results — real people's server IPs. Never commit these.
servers.jsonl
*.jsonl

# Local bookmarks and preferences — per-machine, not part of the project
favorites.json
settings.json

# PyInstaller build output
build/
dist/
*.spec

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Editors / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
