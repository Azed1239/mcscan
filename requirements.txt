# The scanner (mcscan.py) and CLI need nothing but the Python standard library.
# These are only for the GUI and for building the .exe.

customtkinter>=5.2      # dark, rounded desktop UI
pillow>=10.0            # icon generation (make_icon.py)
pyinstaller>=6.0        # optional: build a standalone .exe (build.bat)
